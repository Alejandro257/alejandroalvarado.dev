---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="Alejandro Alvarado - Data Scientist and Machine Learning Engineer focused on Industrial AI, Predictive Analytics and Control Systems."
		/>
		<title>Alejandro Alvarado | Data Scientist & Machine Learning Engineer</title>
	</head>

	<body>
		<header class="navbar">
			<a href="/" class="logo">Alejandro Alvarado</a>

			<nav>
				<a href="#projects">Projects</a>
				<a href="#research">Research</a>
				<a href="#skills">Skills</a>
				<a href="#contact">Contact</a>
			</nav>
		</header>

		<main>
			<section class="hero">
				<div class="hero-content">
					<p class="eyebrow">Data Science · Machine Learning · Industrial AI</p>

					<h1>
						Electronic Engineer focused on Data Science, AI and Control Systems.
					</h1>

					<p class="hero-description">
						I build data-driven solutions using machine learning, predictive analytics,
						optimization and industrial systems knowledge to support better technical and
						strategic decision-making.
					</p>

					<div class="hero-actions">
						<a href="#projects" class="btn primary">View Projects</a>
						<a href="#contact" class="btn secondary">Contact Me</a>
					</div>
				</div>

				<div class="hero-card">
					<p class="card-label">Current Focus</p>
					<h2>Industrial AI & Predictive Analytics</h2>
					<p>
						Machine learning, fuzzy control, optimization, predictive maintenance and
						data visualization for industrial and technological applications.
					</p>
				</div>
			</section>

			<section class="section" id="projects">
				<div class="section-header">
					<p class="eyebrow">Portfolio</p>
					<h2>Featured Projects</h2>
					<p>
						A selection of projects focused on data science, machine learning,
						industrial analytics and model deployment.
					</p>
				</div>

				<div class="grid">
					<article class="project-card">
						<span>Machine Learning</span>
						<h3>Battery RUL Prediction</h3>
						<p>
							Predictive model for estimating the remaining useful life of batteries
							using data analysis and machine learning techniques.
						</p>
					</article>

					<article class="project-card">
						<span>Industrial Analytics</span>
						<h3>Industrial Energy EDA</h3>
						<p>
							Exploratory data analysis of industrial energy, UPS monitoring and
							technical operation variables.
						</p>
					</article>

					<article class="project-card">
						<span>Research</span>
						<h3>Fuzzy Control Optimization</h3>
						<p>
							Multi-objective optimization of TSK-1 fuzzy controllers for MIMO control
							systems using Pareto-based performance analysis.
						</p>
					</article>
				</div>
			</section>

			<section class="section split" id="research">
				<div>
					<p class="eyebrow">Research</p>
					<h2>Research and Academic Work</h2>
				</div>

				<div class="content-box">
					<h3>Optimization, Artificial Intelligence and Control</h3>
					<p>
						My research interests include data science, artificial intelligence,
						multi-objective optimization, fuzzy control systems and industrial process
						optimization.
					</p>
				</div>
			</section>

			<section class="section" id="skills">
				<div class="section-header">
					<p class="eyebrow">Technical Stack</p>
					<h2>Skills and Tools</h2>
				</div>

				<div class="skills">
					<span>Python</span>
					<span>Pandas</span>
					<span>NumPy</span>
					<span>scikit-learn</span>
					<span>PyTorch</span>
					<span>Matplotlib</span>
					<span>SQL</span>
					<span>Power BI</span>
					<span>Grafana</span>
					<span>MATLAB</span>
					<span>Control Systems</span>
					<span>Industrial Automation</span>
				</div>
			</section>

			<section class="section contact" id="contact">
				<p class="eyebrow">Contact</p>
				<h2>Available for remote opportunities and research collaborations.</h2>
				<p>
					Let’s connect to discuss data science, machine learning, industrial AI or
					control systems projects.
				</p>

				<div class="contact-links">
					<a href="mailto:alvaradoalejandro19@outlook.com">Email</a>
					<a href="https://www.linkedin.com/in/alejandro-alvarado-6466612a9" target="_blank">
						LinkedIn
					</a>
					<a href="https://github.com/Alejandro257" target="_blank">GitHub</a>
				</div>
			</section>
		</main>

		<footer>
			<p>© 2026 Alejandro Alvarado. Built with Astro.</p>
		</footer>
	</body>
</html>

<style>
	:root {
		--bg: #07111f;
		--bg-soft: #0f1b2e;
		--card: #111827;
		--text: #f8fafc;
		--muted: #94a3b8;
		--primary: #38bdf8;
		--primary-dark: #0284c7;
		--border: rgba(148, 163, 184, 0.18);
	}

	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	html {
		scroll-behavior: smooth;
	}

	body {
		font-family:
			Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
			sans-serif;
		background:
			radial-gradient(circle at top left, rgba(56, 189, 248, 0.16), transparent 32%),
			var(--bg);
		color: var(--text);
		line-height: 1.6;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	.navbar {
		width: min(1120px, calc(100% - 40px));
		margin: 0 auto;
		padding: 28px 0;
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	.logo {
		font-weight: 800;
		font-size: 1.05rem;
		letter-spacing: -0.03em;
	}

	nav {
		display: flex;
		gap: 24px;
		color: var(--muted);
		font-size: 0.95rem;
	}

	nav a:hover {
		color: var(--primary);
	}

	main {
		width: min(1120px, calc(100% - 40px));
		margin: 0 auto;
	}

	.hero {
		min-height: 78vh;
		display: grid;
		grid-template-columns: 1.35fr 0.65fr;
		gap: 48px;
		align-items: center;
		padding: 72px 0;
	}

	.eyebrow {
		color: var(--primary);
		text-transform: uppercase;
		letter-spacing: 0.14em;
		font-size: 0.78rem;
		font-weight: 800;
		margin-bottom: 16px;
	}

	h1 {
		font-size: clamp(2.8rem, 7vw, 5.8rem);
		line-height: 0.95;
		letter-spacing: -0.08em;
		max-width: 900px;
		margin-bottom: 28px;
	}

	.hero-description {
		max-width: 690px;
		color: var(--muted);
		font-size: 1.15rem;
		margin-bottom: 34px;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 14px;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 48px;
		padding: 0 22px;
		border-radius: 999px;
		font-weight: 700;
		border: 1px solid var(--border);
	}

	.btn.primary {
		background: var(--primary);
		color: #04111f;
		border-color: var(--primary);
	}

	.btn.primary:hover {
		background: #7dd3fc;
	}

	.btn.secondary {
		color: var(--text);
		background: rgba(255, 255, 255, 0.03);
	}

	.btn.secondary:hover {
		border-color: var(--primary);
		color: var(--primary);
	}

	.hero-card,
	.project-card,
	.content-box {
		border: 1px solid var(--border);
		background: rgba(15, 27, 46, 0.72);
		backdrop-filter: blur(16px);
		border-radius: 28px;
		padding: 28px;
		box-shadow: 0 24px 80px rgba(0, 0, 0, 0.2);
	}

	.hero-card {
		align-self: end;
	}

	.card-label {
		color: var(--primary);
		font-size: 0.85rem;
		font-weight: 800;
		margin-bottom: 16px;
	}

	.hero-card h2,
	.section h2 {
		font-size: clamp(2rem, 4vw, 3.2rem);
		line-height: 1;
		letter-spacing: -0.06em;
		margin-bottom: 18px;
	}

	.hero-card p,
	.section-header p,
	.project-card p,
	.content-box p,
	.contact p {
		color: var(--muted);
	}

	.section {
		padding: 90px 0;
		border-top: 1px solid var(--border);
	}

	.section-header {
		max-width: 760px;
		margin-bottom: 34px;
	}

	.grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 20px;
	}

	.project-card {
		transition:
			transform 0.2s ease,
			border-color 0.2s ease;
	}

	.project-card:hover {
		transform: translateY(-6px);
		border-color: rgba(56, 189, 248, 0.55);
	}

	.project-card span {
		display: inline-block;
		color: var(--primary);
		font-size: 0.78rem;
		font-weight: 800;
		margin-bottom: 18px;
	}

	.project-card h3,
	.content-box h3 {
		font-size: 1.25rem;
		margin-bottom: 12px;
	}

	.split {
		display: grid;
		grid-template-columns: 0.9fr 1.1fr;
		gap: 34px;
		align-items: start;
	}

	.skills {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
	}

	.skills span {
		padding: 10px 14px;
		border: 1px solid var(--border);
		border-radius: 999px;
		color: var(--text);
		background: rgba(255, 255, 255, 0.04);
	}

	.contact {
		text-align: center;
		max-width: 860px;
		margin: 0 auto;
	}

	.contact-links {
		display: flex;
		justify-content: center;
		flex-wrap: wrap;
		gap: 14px;
		margin-top: 28px;
	}

	.contact-links a {
		padding: 12px 18px;
		border-radius: 999px;
		background: var(--card);
		border: 1px solid var(--border);
		color: var(--primary);
		font-weight: 700;
	}

	.contact-links a:hover {
		border-color: var(--primary);
	}

	footer {
		width: min(1120px, calc(100% - 40px));
		margin: 0 auto;
		padding: 32px 0;
		border-top: 1px solid var(--border);
		color: var(--muted);
		font-size: 0.9rem;
	}

	@media (max-width: 860px) {
		.navbar {
			align-items: flex-start;
			gap: 18px;
			flex-direction: column;
		}

		nav {
			flex-wrap: wrap;
			gap: 14px;
		}

		.hero,
		.split {
			grid-template-columns: 1fr;
		}

		.grid {
			grid-template-columns: 1fr;
		}

		.hero {
			padding: 48px 0;
		}

		.section {
			padding: 64px 0;
		}
	}
</style>